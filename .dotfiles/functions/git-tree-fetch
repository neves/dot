# function git-tree-fetch {

(
  if [[ $(git rev-parse --show-toplevel 2> /dev/null) ]]; then
    echo $PWD: git fetch ...
    git fetch
  else
    for f in */(N); do
      if [ "$f" == 'node_modules' ]; then
        cd $f
        git-tree-fetch
        cd ..
      fi
    done
  fi
)
# }
